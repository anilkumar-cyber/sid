services:
  postgres:
    image: postgres:16-alpine
    container_name: sidbwood_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-sidbwood}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-sidbwood}
      POSTGRES_DB: ${POSTGRES_DB:-sidbwood}
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-sidbwood}"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: ./apps/api
    container_name: sidbwood_api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER:-sidbwood}:${POSTGRES_PASSWORD:-sidbwood}@postgres:5432/${POSTGRES_DB:-sidbwood}
    ports:
      - "8010:8000"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./apps/api:/app
      - api_storage:/app/storage

  web:
    build:
      context: ./apps/web
    container_name: sidbwood_web
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000/api/v1
    ports:
      - "3000:3000"
    depends_on:
      - api

volumes:
  pgdata:
  api_storage:
